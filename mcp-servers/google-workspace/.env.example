# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here

# Server Configuration
PORT=3000
BASE_URL=http://localhost:3000

# IMPORTANT: OAuth Redirect URI Setup
# Add BOTH redirect URIs to your Google Cloud Console OAuth client:
#   {BASE_URL}/auth/callback          — for MCP protocol clients (Claude, Cursor)
#   {BASE_URL}/auth/app/callback      — for mobile / REST apps
#
# Example (local dev):
#   http://localhost:3000/auth/callback
#   http://localhost:3000/auth/app/callback
#
# Example (Cloud Run):
#   https://google-mcp-xxx.region.run.app/auth/callback
#   https://google-mcp-xxx.region.run.app/auth/app/callback
#
# Go to: https://console.cloud.google.com/apis/credentials
# Edit your OAuth 2.0 Client → Add both URIs → Save
