version: '3.8'

services:
  travel-mcp:
    build: .
    ports:
      - "3001:3001"
    environment:
      - SERPAPI_KEY=${SERPAPI_KEY}
      - API_KEY=${API_KEY}
      - PORT=3001
      - BASE_URL=http://localhost:3001
    env_file:
      - .env
    restart: unless-stopped
