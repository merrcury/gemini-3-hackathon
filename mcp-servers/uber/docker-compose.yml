version: '3.8'

services:
  uber-mcp:
    build: .
    ports:
      - "3002:3002"
    environment:
      - UBER_SERVER_TOKEN=${UBER_SERVER_TOKEN}
      - PORT=3002
      - BASE_URL=http://localhost:3002
    env_file:
      - .env
    restart: unless-stopped
